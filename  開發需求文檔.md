# 開發需求文檔：Hermes Agent 本地照片預處理與雲端標註系統

## 1. 專案背景
目標是建立一個基於 M4 Mac Mini (16GB RAM) 的照片處理管線。為了節省雲端 Vision API 的 Token 消耗並保護隱私，需先在本地進行向量化與聚類，僅挑選代表性照片送往雲端標註。

## 2. 開發目標
請根據以下規格編寫三個獨立的 Python 模組。

---

### 模組一：Local_Indexer.py (本地向量化與特徵提取)
**功能描述：** 遍歷指定目錄，利用本地模型將照片轉換為特徵向量。
*   **核心庫：** `open_clip`, `torch`, `Pillow`
*   **硬體優化：** 必須強制使用 Apple Silicon MPS (Metal Performance Shaders) 加速。
*   **技術要求：**
    *   實作遞歸掃描，支援 .jpg, .jpeg, .png, .webp 格式。
    *   使用 ViT-H-14 或同等級別的 CLIP 模型生成 Embedding。
    *   將結果持久化存儲為單一文件（如 embeddings.pkl 或使用 FAISS 索引），結構需包含「檔案路徑」與「向量」。
    *   需具備錯誤處理機制（跳過損壞的圖檔）。

---

### 模組二：Smart_Sampler.py (語義聚類與代表採樣)
**功能描述：** 分析本地生成的向量，將視覺相似的照片歸類，並挑選最具代表性的照片。
*   **核心庫：** `scikit-learn`, `numpy`
*   **技術要求：**
    *   讀取 Local_Indexer.py 產出的數據。
    *   實作聚類演算法（建議使用 DBSCAN 以自動判定類別數量，或使用自適應 K-Means）。
    *   採樣邏輯：從每個聚類 (Cluster) 中，挑選最接近中心點 (Centroid) 的 N 張照片（N 可配置，預設為 1）。
    *   輸出一個清單 (JSON 格式)，包含所有採樣照片的絕對路徑。

---

### 模組三：Xiaomi_Labeler.py (隱私脫敏與雲端接口)
**功能描述：** 對採樣照片進行物理脫敏處理，並調用小米 Vision API 獲取標註。
*   **核心庫：** `requests`, `PIL` (Pillow)
*   **技術要求：**
    *   隱私保護：上傳前必須執行以下操作：
        1. 移除所有 EXIF 數據（特別是 GPS 座標與拍攝時間）。
        2. 將圖片等比例縮放至長邊不超過 1024px。
    *   API 串接：封裝小米 Vision API 調用，發送 Base64 編碼的圖片數據。
    *   輸出格式：獲取 API 回傳的標註後，輸出為標準化 JSON。

---

## 3. 代碼品質要求
1. 模組化：每個功能需封裝成 Class 或 Function，方便 Hermes Agent 調用。
2. 資源控制：考慮到 16GB 記憶體限制，大數據量處理需使用 Batch Processing。
3. 日誌紀錄：加入清晰的 Logging 資訊。

---

